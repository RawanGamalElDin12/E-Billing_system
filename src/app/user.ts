export class User {
    email: string;
    id: number;
    name: string;
    password: number;
  
    constructor(email: string, id: number, name: string, password: number) {
      this.email = email;
      this.id = id;
      this.name = name;
      this.password = password;
    }
    
  }
  